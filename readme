一：
	aggregate_travel_time.py
	aggregate_volume.py
	两个官方脚本稍做了点修改，便于后面的数据处理，这两个脚本生成的training_20min_avg_volume.csv和		    training_20min_avg_travel_time.csv给第二步用

二：
	KDD_data_extract.ipynb
	读入第一步产生的数据，生成拓展了特征的原始数据travel_time_data.csv和volume_data.csv

三：
	KDD_data_clean.ipynb
	读入travel_time_data.csv和volume_data.csv，暂时先处理了风向的异常值，前20分钟窗口平均值的缺失值暂没处理


其他：
	前面的过程都是为了产生travel_time_data.csv和volume_data.csv，这两个文件都是已经处理好的，可以直接用就好，不用管前面的过程
	KDD_data_analyze.ipynb是之前分析数据用的
